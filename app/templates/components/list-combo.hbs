<select {{action "selectValue"  on="change"}} class="form-control">
  <option selected> {{this.defaultLabel}} </option>
  {{#each this.items as |item|}}
    <option value={{item.id}} >{{item.name}}</option>
  {{/each}}
</select>
